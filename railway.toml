[build]
builder = "NIXPACKS"

[build.buildCommand]
cmd = "npm install && npx playwright install chromium --with-deps"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
