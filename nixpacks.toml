[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "nspr", "atk", "cups", "dbus", "expat", "libdrm", "libxcb", "libxkbcommon", "mesa", "pango", "cairo", "alsa-lib"]

[phases.install]
cmds = ["npm install", "npx playwright install chromium"]

[start]
cmd = "npm start"
